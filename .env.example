# Copy this file to .env and replace every changeme value before Docker paper.
# Hyperliquid wallet credentials are read from config/live_config.json, not .env.

# PostgreSQL passwords
POSTGRES_PASSWORD=changeme_admin
POSTGRES_WRITER_PASSWORD=changeme_writer
POSTGRES_READER_PASSWORD=changeme_reader
